#layout_default
  div(v-if="session")
    p {{user_name()}}
    a(href="#/")
      ns-button(type="primary" size="sm" @click="logout") {{$t('log_in.log_out')}}
  div(v-else="session")
    a(href="#/login")
      ns-button(type="primary" size="sm") {{$t('log_in.log_in')}}
  router-view
