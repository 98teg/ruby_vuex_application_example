div
  p Buscar por título:
    input(v-model="filterTitle")
  p Buscar por contenido:
    input(v-model="filterContent")
  p Buscar por id del autor:
    input(v-model="filterUserID")
  p Desde:
    input(type="date", v-model="filterSince")
  p Until:
    input(type="date", v-model="filterUntil")

  button(type="button", v-on:click="getPosts") Aplicar filtros

  h4 Lista de posts
  table
    tr
      th(v-on:click="orderByTitle") Título
      th(v-on:click="orderByDate") Fecha de creación
      th(v-on:click="orderByAuthor") Autor
      th ID del autor
      th Contenido
    tr(v-for="post in posts")
      td {{post.title}}
      td {{post.creation}}
      td {{post.author}}
      td {{post.user_id}}
      td {{post.content}}
